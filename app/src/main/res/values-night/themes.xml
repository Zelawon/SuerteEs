<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Parent theme for both light and dark modes -->
    <style name="Base.Theme.SuerteEs" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize the light theme here -->
        <!--
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorPrimaryVariant">@color/my_light_primary_variant</item>
        <item name="colorSecondary">@color/my_light_secondary</item>
        <item name="colorSurface">@color/my_light_surface</item>
        <item name="colorBackground">@color/my_light_background</item>
        -->
        <item name="colorPrimary">#00b4d8</item>

    </style>

    <!-- Dark mode theme -->
    <style name="Theme_SuerteEs_Dark" parent="Base.Theme.SuerteEs">
        <!-- Additional customizations if needed-->
    </style>

</resources>
